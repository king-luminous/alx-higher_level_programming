#!/usr/bin/python3
def multiple_returns(sentence):
    for i in len(sentence):
        if i == 0:
            return "none"
        else:
            if i = len(sentence):
                return i, sentence[0]

